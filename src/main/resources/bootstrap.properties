server.port=8080
spring.application.name=${project.name}

# 运行环境必须的编码设置
spring.mandatory-file-encoding=UTF-8

version1=${project.group}:${project.name}:${project.version}
version2=${project.springBoot}
version3=${project.gson}
version4=${project.logback}